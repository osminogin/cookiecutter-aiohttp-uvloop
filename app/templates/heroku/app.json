{
  "name": "Heroku aiohttp with uvloop app",
  "description": "A barebones aiohttp app using gunicorn with uvloop",
  "repository": "https://github.com/osminogin/cookiecutter-aiohttp-uvloop",
  "keywords": ["python", "aiohttp", "asyncio", "uvloop"],
  "buildpacks": [
    {"url": "https://github.com/heroku/heroku-buildpack-multi"}
  ],
  "env": {
    "ROOT_LOG_LEVEL": {
      "description": "Application log level. One of (`CRITICAL`, `ERROR`, `WARNING`, `INFO`, `DEBUG`)",
      "value": "INFO"
    }
  }
}